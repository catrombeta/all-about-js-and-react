const frutas = ['Pêra', 'Maçã', 'Uva'];

// outro exemplo de for para LEITURA e não INCREMENTO. Ou seja, lê os índices ou objeto

for (let i in frutas) {
    console.log(i);
    console.log(frutas[i]);
} 

